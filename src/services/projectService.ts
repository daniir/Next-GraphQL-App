import { Project } from '../graphql/entities/projectEntity';
import { ProjectInput } from '../graphql/inputs/projectInputs';

export class ProjectService
{
    // async getProjects(){
    //     try {
    //         const projects = await 
    //     } catch (error) {
    //         console.error(`Error services get: ${error}`);
    //     }
    // };
};